specVersion: "v1beta"
serviceId: "de-lit-backend"
location: "asia-south1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "postgres"
      cloudSql:
        instanceId: "de-lit-backend-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
